/**
 * Open-source feature stubs
 *
 * When the SaaS package is present, the @saas webpack alias
 * redirects all imports to the real SaaS implementations.
 * When absent (standalone/OS mode), imports resolve here.
 */
export const isSaas = false;
